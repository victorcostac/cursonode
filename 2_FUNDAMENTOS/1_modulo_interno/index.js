const meuModulo = require ("./meu_modulo")

meuModulo.soma(10,20)  //posso usar a função normalmente   

soma = meuModulo.soma // ou jogar a função pra dentro de uma variável e usar a variável para executar a função
soma(20,20)
soma(50,50)
